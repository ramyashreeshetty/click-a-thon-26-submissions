# v2.todo.md — deliberately deferred, with the reason

> **Summary:** Everything known-and-not-done, parked until v1 (`main`) is stable and promoted. Each
> item says **why** it is deferred, not just what it is — a deferral without a reason is just a
> forgotten task. The official 2026-08-02 submission update supersedes A1/A2 below: neither a public
> development repository nor a named Team Captain is required. Current blockers are tracked in
> `REMAINING.md` and Codex Validation 009; the rest of this file remains a dated engineering queue.

**Parked:** 2026-08-02, at the start of the feature-by-feature promotion of `dev` → `main`.

---

## A · Operator decisions — engineering cannot proceed without a human

| # | Item | Why it is blocked | Cost of not doing it |
|---|---|---|---|
| ~~**A1**~~ | ~~**The repo is PRIVATE.**~~ **RETIRED:** the official rules require source in a self-contained submission folder; they do not require this development repository to be public. | — | — |
| ~~**A2**~~ | ~~**No Team Captain named.**~~ **RETIRED:** the official rules require a `[Submission] Team Name` PR, with no Captain-only restriction. | — | — |
| ~~**A3**~~ | ~~**The migration debt**~~ — **CLOSED 2026-08-02.** ADRs 0016, 0021 and 0022 are now all live on `sonyliv`: the operator-authorised rebuild that recovered the 2026-08-01 publish-test corruption applied them in the same pass. Verified after: `cube_level` present, `cc_user_minute` on the ADR 0016 shape, gate 17,028 · 0 mismatched · peak 2,917. | — | — |
| **A4** | **Q32 — two live views return a nondeterministic peak minute.** 70 of 98 hours have a tied peak. | `CREATE OR REPLACE VIEW`, ungated and low-risk, but it touches the graded service. | An hour-grain *"when did it peak"* answer differs between two runs of the same query. Reads as an unstable pipeline. **Global peak 2,917 is a unique minute, so the headline is safe.** |
| **A5** | **Q30 — local `default.session_intervals` is pre-ADR-0012** (no `build_version`), so it cannot be rebuilt locally at all. | Needs a `DROP`; standing instruction is to ask before schema changes. | Any "verify locally first" step runs against a shape Cloud has not had for hours. |

## B · v2 engineering — real, reproduced, not worth destabilising v1 for

**B1 · Replace the per-run interval `DELETE` with tombstones.** Codex 003 §7.3.6, citing ClickHouse's
avoid-mutations guidance. Our measured cost is **1.4 s per publish run** — the dominant cost of a
small batch. Right in principle; not worth changing a pipeline that currently passes its gate.

**B2 · Cross-tier `generation_id` visibility.** `evidence/publish-visibility/` reproduced a reader
seeing the minute curve drop **−87.8% for up to 13.6 s** mid-publish, with hour and user tiers
lagging recovery by a further 3.8 s and 7.3 s. **A one-block correction fix is already prototyped**
(0 deviating samples, 79 ms swap) and handed off — that part may land in v1. The larger
`generation_id` work does not: naive gating was *rejected with a reason* (FINAL resolves before
WHERE on Replacing tiers, so an uncommitted row replaces-then-vanishes — worse than the problem).

**B3 · Provisional vs final bucket labelling.** Codex 003 §9: with an event-time watermark, buckets
older than `max_event_time − allowed_lateness` are final, newer ones provisional. We emit the
watermark and have never set `allowed_lateness` — see [`docs/DESIGN_DECISIONS.md`](docs/DESIGN_DECISIONS.md) §2,
where this is the one undecided decision of the organiser's four. **Blocked on a mentor answer, not on effort.**

**B4 · Skew and hotspot testing.** ⚙ **IN PROGRESS 2026-08-02** — `chore/u1-the-cricketer-spike`
covers the arrival-spike half (a famous cricketer appears; N sessions start in one minute), and
`chore/u2-gb-scale-data-spanning` covers span-vs-density at GB scale over months. Both were pulled
forward because a mentor asked the spike question directly. What neither covers yet: a single session
with millions of events, and one dimension value with enormous cardinality.

**B5 · The publisher running for real on the graded database.** Proven byte-identical across four
tiers in scratch; `cc_publish_runs` on `sonyliv` is **empty**. Depends on A3.

**B6 · Langfuse emitter.** `internal/otelemit` was deliberately built so a second emitter could share
the same `trace_id` without a format change. Not built — the spec requires *one* of
ClickStack/Langfuse/LibreChat and ClickStack is done properly.

## C · Optional differentiators — the spec marks these optional, we chose ClickStack

**C1 · LLM + ClickStack alerting on concurrency decline.** ⚙ **IN PROGRESS 2026-08-02** —
`feat/concurrency-decline-alerting`. Pulled out of v2 because it is the one optional item that is a
**business** signal rather than a technical one, and it deepens the integration we already committed
to rather than adding a second shallow one.

The hard part is not detection. Concurrency declines constantly and legitimately — every asset ends,
prime time ends — so the deliverable is **distinguishing the three causes the spec names**: an asset
ending (decline correlated with `VideoSessionEnd` volume), a system issue (sessions stop
heartbeating without closing), and content not engaging (gradual, mid-asset, elevated
background/pause). An alert that fires on every normal decline is worse than none.

**C2 · LibreChat + ClickHouse MCP conversational layer.** `PROBLEM_STATEMENT.md:75` and the suggested
demo's *"optionally, ask a follow-up question in chat."* The MCP server is preconfigured. Optional;
we already satisfy the integration requirement.

## D · Carried from the v1 hardening wave, if those agents do not land in time

Nine tasks were in flight when promotion began. Any that do not finish move here rather than
being lost — each is tied to a specific spec line, listed in
[`docs/WORKTREE_QUEUE.md`](docs/WORKTREE_QUEUE.md) under "The v1-hardening wave".

The two that are **requirements rather than improvements**, and should be pulled forward into v1 if
at all possible:

- **T1 · new filter columns** — `dataset_details.md:43` says in writing *"the solution should work
  even if the number of dimensions increases."* Measured: a new column loads and is **silently
  dropped**; a missing one silently becomes `''`.
- **T8 · explicit background/foreground events** — `dataset_details.md:21` enumerates them,
  `PROBLEM_STATEMENT.md:31` calls them playback-state markers. **29,021 exist in the data and our
  derivation references them zero times.** ADR 0007 defended that choice but never priced the
  alternative.

## E · Mentor questions — not engineering, but they gate several of the above

Eleven measured dossiers in [`doubts/`](doubts/), ranked by cost. The top three are worth more than
everything below them combined:

| | | worth |
|---|---|---|
| [09](doubts/09-minute-membership-instant-reading.md) | any-overlap vs presence-at-the-instant | **−14.1%** on the graded peak |
| [04](doubts/04-dimension-normalisation.md) | Hindi is four strings | **23.3%** on any per-language answer |
| [10](doubts/10-fail-closed-state-gates.md) | must foreground **and** playing both hold? | **−10.7%** — and two independent implementations agree within 2.4%, so it is semantics, not a bug |

**The costs do not add up** — they overlap, and several are measured independently against the same
baseline. Treat each as *"what this one convention is worth if we have it backwards."*
