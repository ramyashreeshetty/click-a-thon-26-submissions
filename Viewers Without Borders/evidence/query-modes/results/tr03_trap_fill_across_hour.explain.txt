Expression (Project names)
  Sorting (Sorting for ORDER BY)
    Expression ((Before ORDER BY + (Projection + )))
      Expression (Post Join Actions)
        Join (JOIN FillRightFirst)
          Expression (Left Pre Join Actions)
            Expression (Post Join Actions)
              Join (JOIN FillRightFirst)
                Expression (Left Pre Join Actions)
                  Filter ((WHERE + (Change column names to column identifiers + Project names)))
                    Filling
                      Expression ((Before INTERPOLATE + (Before ORDER BY + (Projection + (Change column names to column identifiers + (Project names + Projection)))) [lifted up part]))
                        Sorting (Sorting for ORDER BY)
                          Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Project names + Projection)))))
                            Window (Window step for window 'PARTITION BY toStartOfHour(__table3.minute) ORDER BY __table3.minute ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW')
                              Sorting (Sorting for window 'PARTITION BY toStartOfHour(__table3.minute) ORDER BY __table3.minute ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW')
                                Expression ((Before WINDOW + (Change column names to column identifiers + (Project names + Projection))))
                                  Aggregating
                                    Expression (Before GROUP BY)
                                      Expression ((WHERE + Change column names to column identifiers))
                                        ReadFromMergeTree (sonyliv_u3.cc_minute_delta)
                                        Indexes:
                                          MinMax
                                            Keys:
                                              minute
                                            Condition: and((minute in (-Inf, 1784901599]), (minute in [1784894400, +Inf)))
                                            Parts: 1/7
                                            Granules: 1/10
                                          Partition
                                            Keys:
                                              toYYYYMMDD(minute)
                                            Condition: and((toYYYYMMDD(minute) in (-Inf, 20260724]), (toYYYYMMDD(minute) in [20260724, +Inf)))
                                            Parts: 1/1
                                            Granules: 1/1
                                          PrimaryKey
                                            Keys:
                                              minute
                                            Condition: and((minute in (-Inf, 1784901599]), (minute in [1784894400, +Inf)))
                                            Parts: 1/1
                                            Granules: 1/1
                                            Search Algorithm: generic exclusion search
                                          Ranges: 1
                Expression (Right Pre Join Actions)
                  Filter ((WHERE + (Change column names to column identifiers + Project names)))
                    Filling
                      Expression ((Before INTERPOLATE + (Before ORDER BY + (Projection + (Change column names to column identifiers + (Project names + Projection)))) [lifted up part]))
                        Sorting (Sorting for ORDER BY)
                          Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Project names + Projection)))))
                            Window (Window step for window 'PARTITION BY toStartOfHour(__table7.minute) ORDER BY __table7.minute ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW')
                              Sorting (Sorting for window 'PARTITION BY toStartOfHour(__table7.minute) ORDER BY __table7.minute ASC ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW')
                                Expression ((Before WINDOW + (Change column names to column identifiers + (Project names + Projection))))
                                  Aggregating
                                    Union
                                      Expression ((Before GROUP BY + (Change column names to column identifiers + (Project names + Projection))))
                                        Aggregating
                                          Expression (Before GROUP BY)
                                            Expression ((WHERE + Change column names to column identifiers))
                                              ReadFromMergeTree (sonyliv_u3.cc_minute_delta)
                                              Indexes:
                                                MinMax
                                                  Keys:
                                                    minute
                                                  Condition: and((minute in (-Inf, 1784901599]), (minute in [1784894400, +Inf)))
                                                  Parts: 1/7
                                                  Granules: 1/10
                                                Partition
                                                  Keys:
                                                    toYYYYMMDD(minute)
                                                  Condition: and((toYYYYMMDD(minute) in (-Inf, 20260724]), (toYYYYMMDD(minute) in [20260724, +Inf)))
                                                  Parts: 1/1
                                                  Granules: 1/1
                                                PrimaryKey
                                                  Keys:
                                                    minute
                                                  Condition: and((minute in (-Inf, 1784901599]), (minute in [1784894400, +Inf)))
                                                  Parts: 1/1
                                                  Granules: 1/1
                                                  Search Algorithm: generic exclusion search
                                                Ranges: 1
                                      Expression ((Before GROUP BY + (Change column names to column identifiers + (Conversion before UNION + (Project names + (Projection + Change column names to column identifiers))))))
                                        ReadFromSystemOne
          Expression (Right Pre Join Actions)
            Expression ((WHERE + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Materialize constants after VIEW subquery + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection)))))))))
              Aggregating
                Filter (((WHERE + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Materialize constants after VIEW subquery + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection)))))))) + (Before GROUP BY + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))
                  ReadFromMergeTree (sonyliv_u3.session_intervals)
                  Indexes:
                    PrimaryKey
                      Condition: true
                      Parts: 1/1
                      Granules: 4/4
                    Ranges: 1
