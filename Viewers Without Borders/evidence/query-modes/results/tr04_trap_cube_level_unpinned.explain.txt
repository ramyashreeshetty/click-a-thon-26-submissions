Expression (Project names)
  Sorting (Sorting for ORDER BY)
    Union
      Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Conversion before UNION + (Project names + Projection))))))
        Aggregating
          Expression ((Before GROUP BY + ((WHERE + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Materialize constants after VIEW subquery + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection)))))))) + (WHERE + Change column names to column identifiers))))
            Filter ((((WHERE + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Materialize constants after VIEW subquery + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection)))))))) + (WHERE + Change column names to column identifiers)))[split])
              ReadFromMergeTree (sonyliv_u3.cc_hour_agg)
              Indexes:
                MinMax
                  Keys:
                    hour
                  Condition: (hour in [1785060000, 1785060000])
                  Parts: 1/1
                  Granules: 4/4
                Partition
                  Keys:
                    toYYYYMM(hour)
                  Condition: (toYYYYMM(hour) in [202607, 202607])
                  Parts: 1/1
                  Granules: 4/4
                PrimaryKey
                  Keys:
                    hour
                  Condition: (hour in [1785060000, 1785060000])
                  Parts: 1/1
                  Granules: 4/4
                  Search Algorithm: generic exclusion search
                Ranges: 1
      Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Conversion before UNION + (Project names + Projection))))))
        Aggregating
          Expression ((Before GROUP BY + ((WHERE + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Materialize constants after VIEW subquery + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection)))))))) + (WHERE + Change column names to column identifiers))))
            Filter ((((WHERE + (Change column names to column identifiers + (Convert VIEW subquery result to VIEW table structure + (Materialize constants after VIEW subquery + (Project names + (Projection + (Change column names to column identifiers + (Project names + Projection)))))))) + (WHERE + Change column names to column identifiers)))[split])
              ReadFromMergeTree (sonyliv_u3.cc_hour_agg)
              Indexes:
                MinMax
                  Keys:
                    hour
                  Condition: (hour in [1785060000, 1785060000])
                  Parts: 1/1
                  Granules: 4/4
                Partition
                  Keys:
                    toYYYYMM(hour)
                  Condition: (toYYYYMM(hour) in [202607, 202607])
                  Parts: 1/1
                  Granules: 4/4
                PrimaryKey
                  Keys:
                    cube_level
                    hour
                  Condition: and((cube_level in [7, 7]), (hour in [1785060000, 1785060000]))
                  Parts: 1/1
                  Granules: 4/4
                  Search Algorithm: generic exclusion search
                Ranges: 1
